

SELECT SUM(FGLOCG.VALOR-COALESCE(FGLOD.VLRGLOSA,0))
FROM FGLOD LEFT JOIN FGLOCG ON FGLOD.CNSGLO=FGLOCG.CNSGLO
WHERE FGLOD.CNSGLO=''

 SELECT FGLOCG.VALOR-SUM(COALESCE(FGLOD.VLRGLOSA,0))  FROM FGLOD LEFT JOIN FGLOCG ON FGLOD.CNSGLO=FGLOCG.CNSGLO AND FGLOCG.CNSFCGLO=FGLOD.CODGLOSA WHERE FGLOD.CNSGLO='0200000050' GROUP BY FGLOCG.VALOR SELECT * FROM FGLOD WHERE CNSGLO='0200000050'  SELECT * FROM FGLOCG WHERE CNSGLO='0200000050'  SELECT SUM(FGLOCG.VALOR)-SUM(COALESCE(FGLOD.VLRGLOSA,0))   FROM FGLOCG LEFT JOIN FGLOD ON FGLOCG.CNSGLO=FGLOD.CNSGLO WHERE FGLOCG.CNSGLO='0200000052' GROUP BY FGLOCG.VALOR SELECT  DISTINCT PROCESO FROM ENT WHERE PROCESO='DEVFACTURAS'